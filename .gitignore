/node_modules
/public/storage
/public/hot
/storage/*.key
/vendor
/.idea
Homestead.json
Homestead.yaml
# Normally, the .env file is also in here.
# But there's nothing to keep secret in there,
# This is just a simple exploration of laravel 
# on a local host.